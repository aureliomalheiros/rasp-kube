# Rasp Kube

## First time

### Into Raspberry

git clone the repository:

```
git clone command
```

Running the command into Raspberry:

```
chmod +x conf.sh
sudo ./conf.sh
```

### Into computer

```bash
chmod +x startup.sh
./startup.sh
```
